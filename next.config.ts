import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com", "img.youtube.com", "lh3.googleusercontent.com"],
  },
};

export default nextConfig;
